# Cloudflare Tunnel token.
# TODO: manual setup required — create a tunnel at one.dash.cloudflare.com >
# Networks > Tunnels, choose "Cloudflared" connector, and copy the token
# shown in the install command (the long string after --token).
TUNNEL_TOKEN=change_me
