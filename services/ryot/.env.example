# Ryot requires a Postgres password and a long random admin access token.
# Generate a token with: openssl rand -hex 32
POSTGRES_PASSWORD=change_me
SERVER_ADMIN_ACCESS_TOKEN=change_me_to_a_long_random_string
TZ=change_me
