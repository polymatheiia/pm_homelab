groups:
  - name: service-availability
    rules:
      - alert: ServiceDown
        expr: probe_success == 0
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "{{ $labels.instance }} is down"
          description: "Blackbox probe to {{ $labels.instance }} has failed for more than 5 minutes."
