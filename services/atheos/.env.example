# Atheos — browser-based cloud IDE (fork of Codiad), on-demand via Sablier (host port 9017)
# No app-level secrets: first run walks through an in-browser setup wizard
# (creates the first user/workspace) at https://atheos.lab. Nothing to generate here.
