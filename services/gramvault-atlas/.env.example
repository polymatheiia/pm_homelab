# Host uid:gid that owns /srv/homelab/gramvault-atlas — the container runs as
# this so the bind-mounted config.yaml / data / library stay writable.
#   id -u ; id -g
GRAMVAULT_UID=1000
GRAMVAULT_GID=1000
